#pragma once

#include <string>

namespace jvl::io {

// Printing utilities
void header(const std::string &, size_t);

void display_lines(const std::string &, const std::string &);

} // namespace jvl::io